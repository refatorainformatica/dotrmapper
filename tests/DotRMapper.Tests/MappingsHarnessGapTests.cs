// Stub gerado pelo enrich-harness — substitua por testes reais.
using Xunit;

namespace DotRMapper.Tests;

public class MappingsHarnessGapTests
{
    [Fact(Skip = "HARNESS_GAP: feature `mappings` — implementar cobertura. Código: src/mappings")]
    public void Feature_Mappings_NeedsUnitCoverage()
    {
        Assert.True(true);
    }
}
