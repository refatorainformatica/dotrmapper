// Stub gerado pelo enrich-harness — substitua por testes reais.
using Xunit;

namespace DotRMapper.Tests;

public class HooksHarnessGapTests
{
    [Fact(Skip = "HARNESS_GAP: feature `hooks` — implementar cobertura. Código: src/hooks")]
    public void Feature_Hooks_NeedsUnitCoverage()
    {
        Assert.True(true);
    }
}
