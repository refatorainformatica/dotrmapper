// Stub gerado pelo enrich-harness — substitua por testes reais.
using Xunit;

namespace DotRMapper.Tests;

public class BasedHarnessGapTests
{
    [Fact(Skip = "HARNESS_GAP: feature `based` — implementar cobertura. Código: src/based")]
    public void Feature_Based_NeedsUnitCoverage()
    {
        Assert.True(true);
    }
}
