// Stub gerado pelo enrich-harness — substitua por testes reais.
using Xunit;

namespace DotRMapper.Tests;

public class MappingHarnessGapTests
{
    [Fact(Skip = "HARNESS_GAP: feature `mapping` — implementar cobertura. Código: src/mapping")]
    public void Feature_Mapping_NeedsUnitCoverage()
    {
        Assert.True(true);
    }
}
