// Stub gerado pelo enrich-harness — substitua por testes reais.
using Xunit;

namespace DotRMapper.Tests;

public class ConvertersHarnessGapTests
{
    [Fact(Skip = "HARNESS_GAP: feature `converters` — implementar cobertura. Código: src/converters")]
    public void Feature_Converters_NeedsUnitCoverage()
    {
        Assert.True(true);
    }
}
