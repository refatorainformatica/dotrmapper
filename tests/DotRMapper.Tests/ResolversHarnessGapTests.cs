// Stub gerado pelo enrich-harness — substitua por testes reais.
using Xunit;

namespace DotRMapper.Tests;

public class ResolversHarnessGapTests
{
    [Fact(Skip = "HARNESS_GAP: feature `resolvers` — implementar cobertura. Código: src/resolvers")]
    public void Feature_Resolvers_NeedsUnitCoverage()
    {
        Assert.True(true);
    }
}
