// Stub gerado pelo enrich-harness — substitua por testes reais.
using Xunit;

namespace DotRMapper.Tests;

public class ConfigurationHarnessGapTests
{
    [Fact(Skip = "HARNESS_GAP: feature `configuration` — implementar cobertura. Código: src/configuration")]
    public void Feature_Configuration_NeedsUnitCoverage()
    {
        Assert.True(true);
    }
}
