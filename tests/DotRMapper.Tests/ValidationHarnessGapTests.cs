// Stub gerado pelo enrich-harness — substitua por testes reais.
using Xunit;

namespace DotRMapper.Tests;

public class ValidationHarnessGapTests
{
    [Fact(Skip = "HARNESS_GAP: feature `validation` — implementar cobertura. Código: src/validation")]
    public void Feature_Validation_NeedsUnitCoverage()
    {
        Assert.True(true);
    }
}
